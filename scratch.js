// We have an array of hero objects
// We want to find the hero object that matches our query of key, value pairs

// .find is being passed a hero object
  // Check if it has the key, value pairs in query
  // If it does, return it

// For all the keys in our query
  // Check if the query key exists in our hero
    // If it does, 
      // Check the value and return true if the value matches
  // Else if it doesn't have the query key, we return false